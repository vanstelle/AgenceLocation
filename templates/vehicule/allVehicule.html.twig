{% extends 'layout.html.twig' %}

{% block title %} {{parent()}} - vehicules {% endblock %}

{% block content %}


 <div class="row d-flex justify-content-center text-center"> 
   {% for vehicule in vehicules %}
  <div class="card m-1" style="width: 18rem;">
            <div class="card-body">
                 <div>
                     <a href="{{ path("vehicule_update",{id:vehicule.id}) }}"><i class="fas fa-edit mx-1 text-success"></i></a>
                     <a href="{{ path("vehicule_delete", {id:vehicule.id} ) }}"><i class="fas fa-trash mx-1 text-danger" onclick="return confirm(e^tes vous sùr de vouloir supprimer l\'article ?') " ></i></a>
                 </div>

                <h5 class="card-title"> {{ vehicule.titre }}</h5>
                <h6>Marque : {{ vehicule.marque }} </h6>  
                <h6>Modèle : {{ vehicule.model }} </h6>  
                <h6>Modèle : {{ vehicule.description }} </h6>  
                <h6>Prix Journalier : {{ vehicule.prixJournalier }} </h6>  
                <h6 class="card-subtitle mb-2 text-muted">Enregistré le: {{ vehicule.dateEnregistrement | date("d/m/y \à H\\hi") }}</h6>

                {# {% if article.dateDeModification is not null %}
                 <h6 class="card-subtitle mb-2 text-muted">modifié le:  {{article.dateDeModification | date("d\m\y \à H\\hi") }}</h6>
                 
                 {% endif %}
               
                <h6>auteur : </h6>  
                <p class="card-text">{{ article.contenu | slice(0,150) }}....</p>
                <a href="{{ path("app_article",{id:article.id}) }}" class="btn btn-primary card-link">lire l'article</a> #}
            </div>
        </div>
{% endfor %}
</div>  

 {% endblock content %}      



